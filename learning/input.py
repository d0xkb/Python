# inputs
string = input('Insert string: ') # string, previously known as raw_input
integer = int(input('Insert integer: ')) # integer, so 3 or -12
flo = float(input('Insert floa: ')) # float, so 22.4 or 0.1
